import React, { Fragment } from "react";
import { Login } from "../components/AuthForm";

const logInView = () => {
  return (
    <Fragment>
      <Login />
    </Fragment>
  );
};

export default logInView;
